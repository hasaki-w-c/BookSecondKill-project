<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hasaki_w_c.secondkill.dao.OrderItemDAO">
    <select id="findOrderItem" parameterType="String" resultType="com.hasaki_w_c.secondkill.entity.OrderItem">
        select * from t_order_item where orderId = #{value}
    </select>
</mapper>